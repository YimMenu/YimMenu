#include "backend/looped/looped.hpp"
//#include "natives.hpp"
//#include "pointers.hpp"
//#include "gta/replay.hpp"

namespace big
{
	void looped::hud_chat()
	{
		/*if (!g->window.chat && PAD::IS_CONTROL_JUST_PRESSED(0, (int)ControllerInputs::INPUT_MP_TEXT_CHAT_ALL))
		{
			HUD::MULTIPLAYER_CHAT_SET_DISABLED_(true);
	
			g->window.chat = true;
		}
		
		if (g->window.chat && PAD::IS_DISABLED_CONTROL_JUST_PRESSED(0, (int)ControllerInputs::INPUT_FRONTEND_PAUSE_ALTERNATE))
		{
			HUD::MULTIPLAYER_CHAT_SET_DISABLED_(false);
			g->window.chat = false;
		}

		if (g->window.chat) 
		{
			PAD::DISABLE_CONTROL_ACTION(0, (int)ControllerInputs::INPUT_FRONTEND_PAUSE_ALTERNATE, true );
			HUD::SET_PAUSE_MENU_ACTIVE(0);
		}*/
	}
}
